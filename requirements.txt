# このアプリが直接必要とするパッケージの一覧です。
#
# 実際のインストールには requirements.lock（推移的依存を含む全パッケージ）を
# 使用します。本番イメージが「ビルドした日によって中身が変わる」状態を避けるためです。
# ここを編集したら requirements.lock も再生成してください（手順は同ファイルの冒頭）。
#
# 開発・テスト用の依存は requirements-dev.txt にあります。

Flask==3.1.3
Flask-Login==0.6.3
Flask-WTF==1.3.0
gunicorn==26.0.0 # Docker起動用
Werkzeug==3.1.8
SQLAlchemy==2.0.51
PyMySQL==1.2.0
google-genai==2.13.0
google-api-core==2.33.0
googleapis-common-protos==1.75.0
chromadb==1.5.9
rank-bm25==0.2.2
mecab-python3==1.0.12
pydantic==2.13.4
python-dotenv==1.2.2
openpyxl==3.1.5

bcrypt==5.0.0
requests==2.34.2

ollama==0.6.2
